package life.magpies.registry.items.custom

import net.minecraft.item.PickaxeItem
import net.minecraft.item.ToolMaterial

class CitrinePickaxeItem(material: ToolMaterial, attackDamage: Int, attackSpeed: Float, settings: Settings) :
    PickaxeItem(material, attackDamage, attackSpeed, settings)