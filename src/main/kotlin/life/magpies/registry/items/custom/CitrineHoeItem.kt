package life.magpies.registry.items.custom

import net.minecraft.item.HoeItem
import net.minecraft.item.ToolMaterial

class CitrineHoeItem(material: ToolMaterial, attackDamage: Int, attackSpeed: Float, settings: Settings) :
    HoeItem(material, attackDamage, attackSpeed, settings)